# ============================================
# AutomIA Frontend - Railway Configuration
# ============================================

[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
startCommand = "npx serve dist -s -l $PORT"
