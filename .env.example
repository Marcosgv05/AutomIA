# ============================================
# AutomIA - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o arquivo .env no repositório!

# ============================================
# AMBIENTE E SERVIDOR
# ============================================

# Ambiente: development | production
NODE_ENV=development

# Porta do servidor (Railway/Render definem automaticamente via $PORT)
PORT=4000

# URL base do backend (usado para callbacks OAuth)
# Development: http://localhost:4000
# Production: https://seu-backend.railway.app
BASE_URL=http://localhost:4000

# URL do frontend (para redirecionamentos OAuth e CORS)
# Development: http://localhost:5173
# Production: https://seu-frontend.railway.app
FRONTEND_URL=http://localhost:5173

# ============================================
# BANCO DE DADOS (OBRIGATÓRIO)
# ============================================

# PostgreSQL - Railway fornece automaticamente quando você adiciona o serviço
# Formato: postgresql://usuario:senha@host:porta/database
# Railway: Copie DATABASE_URL do serviço PostgreSQL em Variables
DATABASE_URL=postgresql://automia:automia123@localhost:5432/automia

# ============================================
# INTELIGÊNCIA ARTIFICIAL (OBRIGATÓRIO)
# ============================================

# API do Google Gemini para respostas da IA
# Obtenha GRÁTIS em: https://aistudio.google.com/apikey
# Limite gratuito: ~1500 requisições/dia
GEMINI_API_KEY=sua_chave_aqui

# ============================================
# GOOGLE OAUTH (OPCIONAL - para Calendar)
# ============================================

# Obtenha em: https://console.cloud.google.com/apis/credentials
# 1. Crie um projeto
# 2. Ative as APIs: Google Calendar, Google OAuth2
# 3. Crie credenciais OAuth 2.0
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# URIs de callback (atualize com sua URL em produção)
GOOGLE_REDIRECT_URI=http://localhost:4000/calendar/oauth/callback
GOOGLE_AUTH_REDIRECT_URI=http://localhost:4000/auth/google/callback

# ============================================
# SEGURANÇA (IMPORTANTE PARA PRODUÇÃO!)
# ============================================

# CORS - Origens permitidas
# Development: * (permite todas)
# Production: https://seu-frontend.railway.app (apenas seu domínio!)
CORS_ORIGIN=*

# API Key adicional para proteção de endpoints (opcional)
# Deixe vazio para desativar
API_KEY=

# Secret para tokens JWT (TROQUE em produção!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=automia-jwt-secret-change-in-production

# Tempo de expiração do token em dias
TOKEN_EXPIRY_DAYS=7

# ============================================
# DOCKER (apenas para desenvolvimento local)
# ============================================

POSTGRES_USER=automia
POSTGRES_PASSWORD=automia123
POSTGRES_DB=automia
